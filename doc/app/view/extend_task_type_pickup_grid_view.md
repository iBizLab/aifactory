# 扩展任务类型(extend_task_type_pickup_grid_view)  <!-- {docsify-ignore-all} -->


系统自动添加



## 控件
#### CAPTIONBAR(captionbar)
#### 数据表格(grid)
#### 搜索栏(searchbar)
#### 搜索表单(searchform)


### 关联界面行为
  * [扩展任务类型(EXTEND_TASK_TYPE)](module/Base/extend_task_type) : [表格界面_删除操作](module/Base/extend_task_type#界面行为)

<script>
 const { createApp } = Vue
  createApp({
    data() {
      return {

      }
    }
  }).use(ElementPlus).mount('#app')
</script>